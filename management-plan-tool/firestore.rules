rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The Firebase Admin-backed API is the only data boundary. Browser SDKs,
    // authenticated or not, cannot bypass its per-record authorization.
    match /{document=**} { allow read, write: if false; }
  }
}
