import { bootstrap, execute } from '../lib/domain';
import type { State } from '../lib/model';
export const testTime = '2026-09-07T16:00:00.000Z';
export function fixture(): State { let s = bootstrap('admin@example.test', 'admin-uid', 'Alex Executive', 'management', 'Example Organization', testTime); const admin = s.members[0].id; const apply = (type: string, payload: Record<string, unknown>) => { s = execute(s, admin, { type, payload }, testTime); }; apply('team.create', { name: 'Operations' }); apply('team.create', { name: 'Finance' }); const ops = s.teams[0].id; const finance = s.teams[1].id; for (const [email, name, role, teamId] of [['manager@example.test', 'Morgan Manager', 'manager', ops], ['alice@example.test', 'Alice Employee', 'employee', ops], ['bob@example.test', 'Bob Employee', 'employee', ops], ['outsider@example.test', 'Other Manager', 'manager', finance]]) apply('member.save', { email, name, role, teamId, jobTitle: role === 'employee' ? 'Operations specialist' : 'Functional leader' }); for (const m of s.members) m.uid = `${m.email.split('@')[0]}-uid`; const alice = s.members.find(m => m.email.startsWith('alice'))!; apply('plan.create', { ownerId: alice.id, scope: 'individual', title: 'Alice — 90-day operating plan', purpose: 'Deliver reliable operations and resolve service blockers.', horizon: '90-day', cadence: 'Weekly', nextReview: '2026-09-14', visibility: 'participants' }); const bob = s.members.find(m => m.email.startsWith('bob'))!; apply('plan.create', { ownerId: bob.id, scope: 'individual', title: 'Bob — private employee plan', purpose: 'Private Bob expectations.', horizon: 'Quarterly', cadence: 'Weekly', nextReview: '2026-09-14', visibility: 'participants' }); return s; }
export const person = (s: State, who: string) => s.members.find(m => m.email.startsWith(`${who}@`))!;
export const alicePlan = (s: State) => s.plans.find(p => p.ownerId === person(s, 'alice').id)!;
